package game.lettershmess.models;

public class CarbonBasedLifeformPlayer extends BasePlayer{

	public CarbonBasedLifeformPlayer(String name, int playerOrder, ColorTheme theme, int x, int y, int textX, int textY) {
		super(name, playerOrder, theme, x, y, textX, textY);
	}
}
